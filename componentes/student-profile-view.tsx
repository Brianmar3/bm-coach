"use client";
/* eslint-disable @next/next/no-img-element */
import Link from "next/link";
import { useRef, useState } from "react";
import type { PortalProfile } from "@/types/portal";
const showDate = (value: string) => value ? new Date(`${value}T12:00:00`).toLocaleDateString("es-AR") : "Sin definir";
export function StudentProfileView({ profile }: { profile: PortalProfile }) {
  const [photo, setPhoto] = useState(profile.profileImageUrl); const [preview, setPreview] = useState(""); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const input = useRef<HTMLInputElement>(null);
  const initials = `${profile.firstName[0] ?? ""}${profile.lastName[0] ?? ""}`.toUpperCase();
  function choose(file?: File) { setError(""); setMessage(""); if (!file) return; if (file.size > 3 * 1024 * 1024 || !["image/jpeg", "image/png", "image/webp"].includes(file.type)) { setError("Elegí una foto JPG, PNG o WEBP de hasta 3 MB."); return; } if (preview) URL.revokeObjectURL(preview); setPreview(URL.createObjectURL(file)); }
  async function save() { const file = input.current?.files?.[0]; if (!file) return; setSaving(true); setError(""); try { const form = new FormData(); form.set("photo", file); const response = await fetch("/api/portal/profile-photo", { method: "POST", body: form }); const body = await response.json() as { url?: string; message?: string; error?: string }; if (!response.ok || !body.url) throw new Error(body.error ?? "No se pudo guardar la foto."); setPhoto(body.url); setPreview(""); setMessage(body.message ?? "Foto actualizada correctamente."); } catch (reason) { setError(reason instanceof Error ? reason.message : "No se pudo guardar la foto."); } finally { setSaving(false); } }
  async function remove() { if (!window.confirm("¿Eliminar tu foto de perfil?")) return; setSaving(true); setError(""); try { const response = await fetch("/api/portal/profile-photo", { method: "DELETE" }); const body = await response.json() as { message?: string; error?: string }; if (!response.ok) throw new Error(body.error ?? "No se pudo eliminar la foto."); setPhoto(""); setPreview(""); setMessage(body.message ?? "Foto eliminada correctamente."); } catch (reason) { setError(reason instanceof Error ? reason.message : "No se pudo eliminar la foto."); } finally { setSaving(false); } }
  const shown = preview || photo;
  return <div><header className="rounded-2xl border border-zinc-800 bg-zinc-900 p-4 sm:p-5"><div className="flex items-center gap-4"><div className="grid h-20 w-20 shrink-0 place-items-center overflow-hidden rounded-full border border-yellow-400/30 bg-black">{shown ? <img src={shown} alt={`Foto de ${profile.firstName} ${profile.lastName}`} className="h-full w-full object-cover" /> : <span className="text-xl font-bold text-yellow-300">{initials || "BM"}</span>}</div><div className="min-w-0"><h1 className="truncate text-xl font-bold">{profile.firstName} {profile.lastName}</h1><p className="mt-1 text-sm text-zinc-400">{profile.plan || "Plan sin definir"}</p><p className="mt-1 text-xs capitalize text-emerald-300">Alumno {profile.status}</p></div></div><div className="mt-4 flex flex-wrap gap-2"><input ref={input} type="file" accept="image/jpeg,image/png,image/webp" capture="user" onChange={(event) => choose(event.target.files?.[0])} className="sr-only" /><button type="button" disabled={saving} onClick={() => input.current?.click()} className="rounded-lg border border-zinc-700 px-3 py-2 text-sm text-yellow-300">Cambiar foto</button>{preview && <button type="button" disabled={saving} onClick={save} className="rounded-lg bg-yellow-400 px-3 py-2 text-sm font-bold text-black">{saving ? "Guardando…" : "Guardar foto"}</button>}{photo && <button type="button" disabled={saving} onClick={remove} className="rounded-lg px-3 py-2 text-sm text-red-300">Eliminar foto</button>}</div>{error && <p role="alert" className="mt-3 text-sm text-red-300">{error}</p>}{message && <p role="status" className="mt-3 text-sm text-emerald-300">{message}</p>}</header><section className="mt-4 rounded-2xl border border-zinc-800 bg-zinc-900 p-5"><h2 className="font-semibold">Información personal y deportiva</h2><dl className="mt-4 grid gap-4 sm:grid-cols-2"><Item title="Teléfono" value={profile.phone} /><Item title="Correo" value={profile.email || "Sin correo"} /><Item title="Fecha de nacimiento" value={showDate(profile.birthDate)} /><Item title="Objetivo" value={profile.goal || "No definido"} /><Item title="Plan" value={profile.plan} /><Item title="Fecha de ingreso" value={showDate(profile.joinedAt)} /><Item title="Estado" value={profile.status} /></dl><p className="mt-5 text-xs text-zinc-500">Para modificar estos datos, contactá a tu entrenador.</p></section><Link href="/portal/pagos" className="mt-4 flex min-h-12 items-center justify-between rounded-xl border border-yellow-400/20 bg-yellow-400/5 px-4 py-3"><span><strong className="block text-yellow-300">Mi cuota</strong><span className="text-xs text-zinc-500">Estado e historial de pagos</span></span><span>›</span></Link><Link href="/portal/configuracion" className="mt-3 flex min-h-12 items-center justify-between rounded-xl border border-zinc-800 bg-zinc-900 px-4 py-3"><span><strong className="block">Configuración</strong><span className="text-xs text-zinc-500">Cuenta, seguridad y notificaciones</span></span><span className="text-yellow-400">›</span></Link></div>;
}
function Item({ title, value }: { title: string; value: string }) { return <div><dt className="text-xs text-zinc-500">{title}</dt><dd className="mt-1 capitalize">{value || "Sin definir"}</dd></div>; }
